# StudySync frontend configuration
#
# Copy this file to frontend/.env for local development.
# Create React App only exposes variables prefixed with
# REACT_APP_ to the browser.

# Where the API lives. Point this at a shared instance
# for the sprint review instead of localhost.
REACT_APP_API_URL=http://localhost:5000
